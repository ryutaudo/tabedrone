import { getCurrentFridgeContents } from '../../utils/index';

describe('src/utils/index.js', () => {
  describe('getCurrentFridgeContents', () => {
    it('should returns contents data when getting contents succeeded.', (done) => {
      expect(1).toBe(1);
    });
  });
});

